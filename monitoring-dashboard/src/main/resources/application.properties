server.port=9001
spring.application.name=monitoring-dashboard

# Admin UI title
spring.boot.admin.ui.title=Hotel Microservices Dashboard

# Expose all actuator endpoints (Admin server needs this)
management.endpoints.web.exposure.include=health,info,metrics,circuitbreakerevents
management.endpoint.health.show-details=always

# Optional security (for now you can skip or enable)
spring.security.user.name=admin
spring.security.user.password=admin123